# 🚀 النشر التلقائي إلى GitHub

## خطوات النشر (جاهزة للتنفيذ)

تم تضمين توكن GitHub الخاص بك في المشروع للنشر التلقائي.

### الخطوة 1: تحديد اسم المستخدم

قبل التنفيذ، افتح ملف `deploy.sh` وعدّل السطر التالي:

```bash
GITHUB_USERNAME="YOUR_GITHUB_USERNAME"
```

استبدل `YOUR_GITHUB_USERNAME` باسم المستخدم الفعلي في GitHub.

### الخطوة 2: تنفيذ النشر

في Terminal، نفّذ:

```bash
cd maliha-specialty-coffee
./deploy.sh
```

السكريبت سيقوم بـ:
1. إنشاء repository جديد على GitHub
2. رفع جميع الملفات
3. تجهيز المشروع للنشر

### الخطوة 3: تفعيل GitHub Pages

1. اذهب إلى: https://github.com/YOUR_USERNAME/maliha-specialty-coffee/settings/pages
2. في قسم "Source"، اختر `main` branch
3. اضغط "Save"
4. انتظر 1-2 دقيقة

### ✅ الموقع جاهز!

رابط موقعك:
```
https://YOUR_USERNAME.github.io/maliha-specialty-coffee/
```

---

## 🔒 ملاحظة أمنية

التوكن المستخدم لديه صلاحيات على حسابك. للأمان:

1. بعد النشر، يُفضل حذف ملف `deploy.sh` أو حذف التوكن منه
2. أو قم بإنشاء `.gitignore` يتجاهل ملف deploy.sh:
   ```bash
   echo "deploy.sh" >> .gitignore
   git add .gitignore
   git commit -m "Update gitignore"
   git push
   ```

---

☕ مليحة - A Ritual. Not Just Coffee.
